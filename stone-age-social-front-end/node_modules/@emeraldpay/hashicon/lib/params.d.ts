import { Light } from "./sprite";
export interface Range {
    min: number;
    max: number;
}
export interface OptionalParam {
    enabled: boolean;
}
export declare type HasherType = 'blake2' | 'keccak' | 'legacy';
export interface Params {
    hasher: HasherType;
    size: number;
    hue: Range;
    saturation: Range;
    lightness: Range;
    variation: Range & OptionalParam;
    shift: Range;
    figureAlpha: Range;
    light: Record<Light, number> & OptionalParam;
    createCanvas: (width: number, height: number) => HTMLCanvasElement;
}
export declare const DefaultParams: Partial<Params>;
